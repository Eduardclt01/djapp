import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#f5f7f7',
      paper: '#ffff',
    },
    primary: {
      main: '#16697A',
    },
    secondary: {
      main: '#489FB5',
    },
  },
});

export default theme;
